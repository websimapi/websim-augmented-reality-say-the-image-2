{
  "database": {
    "schema": {
      "items_v1": {
        "id": {
          "type": "text",
          "description": "The name of the item, used as a primary key."
        },
        "word": {
          "type": "text",
          "description": "The name of the household item."
        },
        "image_url": {
          "type": "text",
          "description": "URL of the AI-generated image for the item."
        }
      }
    }
  }
}

